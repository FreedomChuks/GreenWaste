<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- This ids are used in both the menu (navigation.xml) and nav graph (nav_graph.xml).
    Each menu item and nav graph fragment represent the same logical item. -->
    <item name="navigation_home" type="id"/>
    <item name="navigation_adview" type="id"/>
    <item name="navigation_profile" type="id"/>
</resources>